# supervisor

Local supervisor for running external coding-agent CLIs as managed background jobs.

Current focus:

- evaluate existing MCP/CLI supervisors for Claude Code delegation
- keep PAL as a synchronous fallback, not a spawn replacement
- decide whether to build a narrow local job MCP for Windows Claude Code + cc-switch

