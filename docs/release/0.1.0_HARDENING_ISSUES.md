# Retinue 0.1.0 Hardening Issues

Date: 2026-05-07

This document records issues found during real Codex plugin smoke and E2E work so they do not disappear while implementation is in progress.

## Release Boundary

0.1.0 default product path:

- Codex plugin marketplace install.
- Default backend: OpenCode.
- Default OpenCode agent: `plan`.
- Claude Code remains experimental and optional.
- New users should not need to compile Retinue or hand-edit backend configuration.

## Known Issues Under Active Fix

### 1. Plugin MCP Startup From Marketplace Cache

Status: fixed in commits `cab6ec0` and `185bd98`.

Evidence:

- Initial Codex error: `MCP startup failed: handshaking with MCP server failed: connection closed: initialize response`.
- Installed plugin cache originally had `.mcp.json` and skills but no usable runtime.
- After runtime copy, cache still failed because the MCP runtime imported dependencies unavailable from the plugin cache.

Required guardrail:

- The plugin cache must start `node ./dist/mcp.js` without relying on repository root files or root `node_modules`.

### 2. Default OpenCode Server Lifecycle

Status: implemented in source; verification in progress.

Problem:

- A one-click plugin install cannot assume the user already started `opencode serve`.
- Retinue should manage the default OpenCode server lifecycle for the plugin path.
- Explicit `RETINUE_OPENCODE_BASE_URL` remains the opt-in attach path for externally managed servers.

Acceptance:

- With plugin defaults and no explicit base URL, Retinue starts OpenCode on loopback.
- The default agent remains `plan`.
- Existing explicit attach behavior still works.
- Startup failures return actionable errors.

### 3. Port Conflicts Across Windows / WSL

Status: implemented in source; verification in progress.

Problem:

- Windows and WSL can both try to use `127.0.0.1:4096`.
- A healthy OpenCode server on `4096` may belong to another OS/user environment. Auto-serve must not blindly attach to it.
- The default path needs a fallback beyond a single port.

Acceptance:

- Retinue-managed auto-serve prefers `4096`.
- If `4096` is occupied by an external service, including an external OpenCode server, Retinue tries fallback ports `4097` through `4127`.
- If all candidate ports are occupied by external services, Retinue reports a clear port conflict.
- If `RETINUE_OPENCODE_PORT` is explicitly set, Retinue does not silently fall back to another port.

### 4. Multiple Codex Threads

Status: implemented in source; verification in progress.

Problem:

- Each Codex thread starts its own Retinue MCP process.
- In-memory tracking of a managed OpenCode server only works within one MCP process.
- Multiple Codex threads need to spawn OpenCode child agents without racing on ports or mistakenly attaching to another environment's OpenCode server.

Implemented design:

- Store Retinue-managed OpenCode server discovery under the Retinue state directory.
- Protect server startup with a cross-process lock.
- Reuse only a server recorded as Retinue-managed and still health-checking.
- Fall back to the next candidate port when the preferred port is occupied by an unowned external service.

Acceptance:

- Two independent Retinue MCP processes can both spawn OpenCode plan subagents.
- The second process reuses the managed server recorded by the first process, or starts a fallback server without port collision.
- Stale discovery is ignored and replaced.

### 5. Empty Result From Real OpenCode Plan Subagent

Status: implemented in source; verification in progress.

Evidence:

- A real Retinue delegation to OpenCode `plan` completed with empty `parsedStdout.result`.

Risk:

- E2E may report a completed job but return no useful answer to Codex.

Acceptance:

- `retinue_wait_agent` returns the assistant text from the OpenCode session for deterministic prompts.
- If OpenCode returns no assistant text, Retinue reports a structured failure or running state instead of a misleading completed empty result.

Implemented behavior:

- A completed assistant message without a visible `text` part does not transition an OpenCode job to `completed`.
- `wait` remains `running` until a visible assistant result exists or the timeout expires.

### 6. Marketplace Cache Refresh Can Leave Other Codex Sessions With Failed Plugin State

Status: root cause identified; fix in progress.

Evidence:

- Local marketplace metadata points at revision `8192b0a4000865d80c2216673c8d2fdfe67caec2`.
- The installed plugin cache and marketplace clone both contain the bundled `plugins/retinue/dist/mcp.js` with OpenCode auto-serve and fallback-port support.
- A direct stdio smoke from `/home/raystorm/.codex/plugins/cache/retinue-local/retinue/0.1.0` can list tools and run an OpenCode spawn/wait/close cycle.
- Codex logs around a plugin reinstall window include `configured non-curated plugin no longer exists in discovered marketplaces during cache refresh` followed by `failed to load plugin: plugin is not installed plugin="retinue@retinue-local"`.
- The current development conversation runs from `/home/raystorm/projects/Retinue`, so `node ./dist/mcp.js` succeeds by resolving the repository root runtime.
- The same command from `/home/raystorm` fails with `Cannot find module '/home/raystorm/dist/mcp.js'`, matching the observed handshake-close symptom in non-Retinue Codex conversations.
- Real `codex mcp get retinue` showed `cwd: -` after the first attempted fix, proving Codex did not bind `./dist/mcp.js` to the plugin cache unless the server config explicitly declared a plugin-relative cwd.
- A local cache diagnostic showed `cwd: "."` resolves to the installed plugin cache path, and real `codex exec --cd /home/raystorm` then completed `retinue_spawn_agent -> retinue_wait_agent -> retinue_close_agent` with `ROOT_CODEX_EXEC_RETINUE_OK`.
- Windows Codex logs later showed cache refresh failing with `failed to back up plugin cache entry: 拒绝访问。 (os error 5)`, followed by `failed to load plugin: missing or invalid plugin.json` for `retinue@retinue-local`. The observed cache directory was empty while config still enabled the plugin, which matches a failed refresh/backup leaving a broken installed-cache entry.
- Retinue now starts through `plugins/retinue/mcp-bootstrap.mjs`: Codex still launches from plugin cache with `cwd: "."` so relative plugin paths resolve, but the bootstrap immediately changes cwd to Retinue state before loading the bundled runtime. This keeps the MCP process from holding the plugin cache directory as its long-lived cwd on Windows.

Risk:

- A Codex session whose working directory is not the Retinue repository can fail to start the Retinue MCP server if plugin-local relative paths are inherited from the conversation cwd.

Guardrail:

- CI must fail if source changes rebuild `dist/` or `plugins/retinue/dist/` without the generated artifacts being committed.
- Plugin MCP startup tests must cover an installed plugin cache launched while the conversation cwd is elsewhere.
- Plugin MCP config must keep `cwd: "."`; removing it makes Codex inherit the conversation cwd and breaks root-directory smoke. The long-lived process cwd should still move out of the plugin cache via the bootstrap.

### 7. OpenCode Auto-Serve Spawn Errors Can Close The MCP Transport

Status: fixed; needs full E2E.

Evidence:

- A Windows Codex E2E reached Retinue tool discovery, then `opencode_run` and `opencode_status` failed with `Transport closed`.
- Source review found `startManagedOpenCodeServer` called `spawn("opencode", ...)` without handling the child process `error` event.
- A regression test using a missing OpenCode command reproduced an unhandled `spawn ... ENOENT` error. That class of failure can terminate the Retinue MCP process instead of returning a tool error.
- A later Windows Retinue E2E returned `Failed to start OpenCode server command "opencode": spawn opencode ENOENT`. `where.exe opencode` found pnpm shims, proving the CLI was installed but not visible to the Codex appserver process PATH. The default install target is the official OpenCode script, which installs under `$HOME/.opencode/bin/opencode`.

Risk:

- If OpenCode is missing from PATH, a Windows `.cmd` shim cannot be resolved, or a configured OpenCode command exits before serving `/global/health`, Codex can lose the whole Retinue MCP transport.

Implemented behavior:

- OpenCode server startup now races health polling against child `error` and early `exit`.
- Startup failures are returned as explicit errors such as `Failed to start OpenCode server command "..."` or `OpenCode server command "..." exited before becoming healthy`.
- `tests/opencode-server-manager.test.ts` covers the missing-command case so this cannot regress silently.
- Retinue resolves the default `opencode` command through inherited PATH and common install locations. On Windows it checks `%USERPROFILE%\.opencode\bin\opencode` before package-manager shim fallbacks, and launches `.cmd`/`.bat` shims through the Windows shell.

## Current Verification Requirements

Before calling 0.1.0 ready:

- `pnpm run typecheck`
- `pnpm run check:generated`
- `pnpm test`
- `pnpm run verify:package`
- Real Retinue OpenCode E2E using `retinue_spawn_agent -> retinue_wait_agent -> retinue_close_agent`
- Marketplace cache MCP startup smoke from the installed plugin cache
